<Page x:Class="SimpleAppWithNoAppXaml.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SimpleAppWithNoAppXaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">

    <Grid Background="Yellow">
        <StackPanel Orientation="Vertical">
            <TextBlock Grid.Row="1" Text="Main View" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Button Command="{Binding GoBack}" IsEnabled="{Binding CanGoBack}" Style="{StaticResource toolBarButtonImage}">
                <TextBlock Text="Button" FontSize="16" />
            </Button>
        </StackPanel>
    </Grid>
</Page>
